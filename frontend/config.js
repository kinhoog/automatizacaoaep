/*
 * Este arquivo é substituído no deploy do GitHub Pages.
 * Configure a variável AEP_API_BASE_URL no repositório; não inclua segredos aqui.
 */
window.AEP_CONFIG = Object.freeze({
  API_BASE_URL: "",
});
